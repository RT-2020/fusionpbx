---
globs: *.js
---

# JavaScript 开发规则

## 代码组织

- 使用 jQuery 作为主要 DOM 操作库
- 事件处理使用 `$(document).ready()`
- AJAX 调用使用 `$.ajax()` 或 `$.post()`

## 命名约定

- 变量使用驼峰命名法：`userId`、`domainName`
- 函数使用驼峰命名法：`getUserInfo()`
- 常量使用大写下划线：`MAX_CONNECTIONS`

## WebSocket 通信

- 使用 WSS（安全 WebSocket）协议
- 实现心跳检测和自动重连机制
- 正确处理连接状态和错误

## 代码示例

### 基本结构

```javascript
$(document).ready(function () {
  // 初始化代码
  initializeWebSocket()
  bindEvents()
})

function initializeWebSocket() {
  // WebSocket 初始化代码
}

function bindEvents() {
  // 事件绑定代码
}
```

### AJAX 请求

```javascript
function fetchData(url, data, callback) {
  $.ajax({
    url: url,
    type: 'POST',
    data: data,
    dataType: 'json',
    success: function (response) {
      if (callback) {
        callback(response)
      }
    },
    error: function (xhr, status, error) {
      console.error('AJAX Error:', error)
      showMessage('请求失败: ' + error, 'error')
    },
  })
}
```

### WebSocket 连接

```javascript
var ws = null
var reconnectInterval = 5000 // 5秒重连间隔

function connectWebSocket() {
  var protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:'
  var wsUrl = protocol + '//' + window.location.host + '/ws'

  ws = new WebSocket(wsUrl)

  ws.onopen = function (event) {
    console.log('WebSocket 连接已建立')
    startHeartbeat()
  }

  ws.onmessage = function (event) {
    var data = JSON.parse(event.data)
    handleMessage(data)
  }

  ws.onclose = function (event) {
    console.log('WebSocket 连接已关闭')
    setTimeout(connectWebSocket, reconnectInterval)
  }

  ws.onerror = function (error) {
    console.error('WebSocket 错误:', error)
  }
}

function startHeartbeat() {
  setInterval(function () {
    if (ws && ws.readyState === WebSocket.OPEN) {
      ws.send(JSON.stringify({ type: 'heartbeat' }))
    }
  }, 30000) // 每30秒发送一次心跳
}
```

## 错误处理

- 使用 try-catch 包装可能失败的操作
- 提供用户友好的错误消息
- 记录详细错误信息到控制台

## 性能优化

- 避免频繁的 DOM 操作
- 使用事件委托处理动态元素
- 合理使用缓存
- 避免内存泄漏

## 安全考虑

- 验证所有用户输入
- 使用 HTTPS/WSS 协议
- 避免在客户端存储敏感信息
- 实施 CSRF 防护
