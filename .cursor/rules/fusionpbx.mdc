---
alwaysApply: true
---

# FusionPBX 项目规则

你是一位精通 PHP、FreeSWITCH、VoIP 技术、PostgreSQL 和企业级通信系统的专家。

## 项目概述

FusionPBX 是一个企业级多租户 PBX 系统，基于 FreeSWITCH 构建，提供完整的电信解决方案。

## 核心技术栈

### 后端技术

- **PHP 7.4+**: 主要编程语言
- **FreeSWITCH**: 核心通信引擎
- **Lua**: FreeSWITCH 脚本语言
- **PostgreSQL**: 主数据库
- **PDO**: 数据库抽象层

### 前端技术

- **JavaScript (ES5/ES6)**: 原生 JavaScript
- **jQuery**: DOM 操作和 AJAX
- **Bootstrap 4/5**: UI 框架
- **Chart.js**: 数据可视化
- **ACE Editor**: 代码编辑器
- **Font Awesome**: 图标库

### 通信协议

- **SIP**: VoIP 信令协议
- **WebSocket/WSS**: 实时通信
- **WebRTC**: 浏览器实时通信

## 代码风格和结构

### PHP 编码规范

1. **文件头部**

   - 所有 PHP 文件必须包含 Mozilla Public License 1.1 许可证头部
   - 包含文件描述、作者信息和版本信息

2. **代码风格**

   - 使用 Tab 缩进（不是空格）
   - 变量使用下划线命名法：`$user_name`、`$domain_uuid`
   - 函数使用下划线命名法：`function get_user_info()`
   - 类名使用下划线命名法：`class database_connection`

3. **安全最佳实践**

   - 始终使用 PDO 预处理语句防止 SQL 注入
   - 使用 `check_str()` 函数清理用户输入
   - 使用 `permission_exists()` 检查权限
   - 使用 `is_uuid()` 验证 UUID 格式
   - 使用 `htmlentities()` 或 `escape()` 转义输出

4. **数据库操作**

   - 优先使用全局 `$database` 对象
   - 使用 `$database->select()` 进行查询
   - 使用 `$database->execute()` 进行增删改操作
   - 所有主键使用 UUID（36 字符）
   - 表名使用 `v_` 前缀

5. **Session 管理**
   - 使用 `$_SESSION` 存储用户状态
   - 多租户使用 `$_SESSION['domain_uuid']`
   - 权限检查使用 `$_SESSION['user']['permissions']`
   - 用户信息存储在 `$_SESSION['user']`

### JavaScript 编码规范

1. **代码组织**

   - 使用 jQuery 作为主要 DOM 操作库
   - 事件处理使用 `$(document).ready()`
   - AJAX 调用使用 `$.ajax()` 或 `$.post()`

2. **命名约定**

   - 变量使用驼峰命名法：`userId`、`domainName`
   - 函数使用驼峰命名法：`getUserInfo()`
   - 常量使用大写下划线：`MAX_CONNECTIONS`

3. **WebSocket 通信**
   - 使用 WSS（安全 WebSocket）协议
   - 实现心跳检测和自动重连机制
   - 正确处理连接状态和错误

### Lua 脚本规范

1. **FreeSWITCH Lua**

   - 使用 `freeswitch.consoleLog()` 记录日志
   - 使用 `session:ready()` 检查会话状态
   - 正确处理 hangup 事件
   - 使用 `api:execute()` 调用 FreeSWITCH API

2. **错误处理**
   - 始终检查 session 是否有效
   - 使用 pcall() 包装可能失败的操作
   - 记录详细的错误信息

## 项目结构

### 目录组织

```
/app/                  # 应用模块
  /{module_name}/      # 单个模块
    app_config.php     # 模块配置
    app_defaults.php   # 默认设置
    app_languages.php  # 多语言支持
    app_menu.php       # 菜单定义
    resources/         # 模块资源文件
/core/                 # 核心功能
/resources/            # 共享资源
  classes/             # PHP 类库
  functions.php        # 全局函数
  require.php          # 自动加载
/themes/               # 主题模板
```

### 模块开发规范

1. **创建新模块**

   - 在 `/app/` 下创建模块目录
   - 必须包含 `app_config.php`
   - 使用 `app_languages.php` 实现国际化
   - 在 `app_menu.php` 中定义菜单项

2. **数据库表**

   - 表名格式：`v_{module_name}`
   - 主键命名：`{table_name}_uuid`
   - 必须包含：`domain_uuid`（多租户）
   - 审计字段：`insert_date`、`insert_user`、`update_date`、`update_user`

3. **权限系统**
   - 权限命名：`{module}_{action}`（如：`extension_view`、`extension_add`）
   - 使用 `permission_exists()` 检查权限
   - 在 `app_config.php` 中定义权限

## VoIP 和通信特定规范

### SIP 和拨号计划

1. **拨号计划开发**

   - XML 拨号计划位于 `/app/dialplans/`
   - 使用条件判断和正则表达式
   - 考虑多租户场景（domain_uuid 隔离）

2. **FreeSWITCH 集成**

   - 使用 Event Socket Layer (ESL) 通信
   - 通过 `switch.php` 执行 FreeSWITCH 命令
   - 正确处理通话状态和事件

3. **设备配置**
   - 模板位于 `/resources/templates/`
   - 支持多品牌设备（Yealink、Grandstream、Cisco 等）
   - 使用变量替换生成配置文件

## 最佳实践

### 性能优化

1. **数据库查询**

   - 避免 N+1 查询问题
   - 使用索引优化查询
   - 对大型结果集使用分页

2. **缓存策略**
   - 使用 `$_SESSION` 缓存常用数据
   - 避免重复查询相同数据
   - 清理过期缓存

### 安全性

1. **认证和授权**

   - 所有管理页面检查登录状态
   - 使用 `require_once "resources/require.php"` 初始化
   - 实施最小权限原则

2. **输入验证**

   - 验证所有用户输入
   - 使用白名单而不是黑名单
   - 验证文件上传类型和大小

3. **日志和审计**
   - 记录重要操作到 `v_user_logs`
   - 记录失败的登录尝试
   - 监控异常活动

### 多租户支持

1. **数据隔离**

   - 所有查询必须包含 `domain_uuid` 过滤
   - 使用 `$_SESSION['domain_uuid']` 获取当前域
   - 检查用户是否有 `domain_all` 权限

2. **配置管理**
   - 使用 `v_default_settings` 存储全局配置
   - 使用 `v_domain_settings` 存储域级配置
   - 优先级：用户设置 > 域设置 > 默认设置

## 调试和测试

### 调试技巧

1. **PHP 调试**

   - 使用 `echo` 或 `print_r()` 输出变量（开发环境）
   - 检查 `/var/log/freeswitch/` 下的日志
   - 使用浏览器开发者工具检查 AJAX 请求

2. **FreeSWITCH 调试**
   - 使用 `fs_cli` 命令行工具
   - 查看 `freeswitch.log` 日志
   - 使用 `sofia status` 检查 SIP 状态

### 错误处理

1. **PHP 错误处理**

   - 使用 try-catch 包装可能失败的操作
   - 提供用户友好的错误消息
   - 记录详细错误信息到日志

2. **用户反馈**
   - 使用 `message::add()` 显示消息
   - 区分错误、警告和成功消息
   - 提供可操作的错误提示

## 国际化

1. **多语言支持**

   - 使用 `app_languages.php` 定义翻译
   - 使用 `$text` 数组访问翻译
   - 格式：`$text['label-key']['en-us'] = "English Text"`

2. **日期和时间**
   - 使用数据库的时区设置
   - 显示时转换为用户时区
   - 使用标准格式存储（ISO 8601）

## 文档和注释

1. **代码注释**

   - 为复杂逻辑添加注释
   - 解释业务规则和约束
   - 标注 TODO 和 FIXME

2. **API 文档**
   - 为公共函数编写文档注释
   - 说明参数类型和返回值
   - 提供使用示例

## 版本控制

1. **Git 工作流**

   - 基于功能分支开发
   - 提交消息要清晰描述变更
   - 避免提交敏感信息（密码、密钥）

2. **忽略文件**
   - 配置文件（包含密码）
   - 临时文件和缓存
   - 用户上传的文件

## 关键约定

- 优先考虑系统的稳定性和可靠性
- 保持向后兼容性
- 遵循 FusionPBX 现有的代码风格
- 考虑多租户场景下的数据隔离
- 实施严格的权限检查
- 编写可维护和可扩展的代码
- 注重电信系统的实时性和高可用性

## 常用函数和工具

### 全局函数（resources/functions.php）

- `permission_exists($permission)` - 检查权限
- `is_uuid($uuid)` - 验证 UUID 格式
- `check_str($str)` - 清理字符串
- `escape($str)` - 转义 HTML
- `format_phone($phone)` - 格式化电话号码
- `byte_convert($bytes)` - 字节单位转换

### 数据库类（resources/classes/database.php）

- `$database->select($sql, $parameters, $mode)` - 查询
- `$database->execute($sql, $parameters)` - 执行
- `$database->count($sql, $parameters)` - 计数

### 设置类（resources/classes/settings.php）

- `$settings->get($category, $subcategory, $name)` - 获取设置
- `$settings->set($category, $subcategory, $name, $value)` - 设置值

参考项目中的现有代码和 FusionPBX 官方文档了解更多最佳实践。
