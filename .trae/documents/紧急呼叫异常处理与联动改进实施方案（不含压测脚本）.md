## 改进项与可行性评估
- 去重防抖（紧急失败事件）
  - 方案：在 `dispatcher-control.js` 的 `handleEmergencyFail(meta)` 引入去重键（优先 `meta.call_id`，回退 `session._customId`），维护 `seenFailures` 的短期窗口（3–5秒 TTL），重复事件直接忽略。
  - 可行性：高。已有 `seenFailures` 字段，事件源携带 `meta`，改动局部且不影响非紧急流程。
- 统一错误提示为模态（替换紧急路径中的 `alert()`）
  - 方案：复用 `#dispatcher-error-modal` 与 `showErrorModal(meta)` 渲染错误提示；在紧急相关的失败/接听异常路径统一调用模态并移除 `alert()`；保留非紧急通用提示暂不改动，以降低风险。
  - 可行性：高。容器与函数已存在，渲染模板完善，改动集中在紧急分支。
- 导出资源报告按钮（调度端）
  - 方案：在 `app/basic_operator_panel/index.php` 增加“导出资源报告”按钮；前端调用 `sipClient.getResourceReport()`，使用 `Blob`/`URL.createObjectURL` 下载 JSON 文件；复用 `dispatcher-logger` 的导出方式以保持一致。
  - 可行性：高。报告接口已实现，UI新增不影响现有功能。
- 文档说明（每个改进点）
  - 方案：在 `.trae/documents` 中为本次改进追加说明段落：用途、启用方式、回退策略、兼容性；并在代码中补充最小必要内联注释（仅关键处，避免噪音）。
  - 可行性：高。用户已明确要求补文档，现有文档结构可直接扩展。
- 代码审查与质量验证机制
  - 方案：建立改动检查清单（入库前执行）：事件去重正确性、模态渲染与隐藏、资源报告数据完整、紧急音频停止与队列清理成功；提供手测用例与浏览器兼容矩阵（Chrome/Edge/Firefox/Safari）。
  - 可行性：高。以现有前端为主，验证成本可控。
- 压测与统计脚本（不采纳）
  - 原因：用户要求明确排除性能压测脚本；同时压测环境依赖较多、收益与当前迭代目标不匹配。将于改进日志中注明并保留后续选项。

## 分阶段实施计划
- 阶段1：事件去重与稳定性护栏
  - 在 `handleEmergencyFail(meta)` 中：计算 `key=meta.call_id || meta.session_id || 'unknown'`；`seenFailures[key]` 命中则跳过；新建时写入并 `setTimeout` 在 5 秒后删除。
  - 为缺失 `meta` 的调用路径（接听失败 `.catch`）注入最小 `meta`：`{cause,timestamp}`。
  - 风险控制：仅作用于紧急失败闭环，不改动常规来话与非紧急分支。
- 阶段2：模态统一与回退机制
  - 紧急相关路径替换 `alert()` 为 `showErrorModal(meta)`；在 `showErrorModal` 增加容器缺失回退（静默或控制台提示），避免硬错误。
  - 保留非紧急场景的 `alert()`，后续按需求再统一。
- 阶段3：资源报告导出
  - 在 `index.php` 紧急控制区添加按钮 `导出资源报告`；前端实现 `exportResourceReport()`：拉取 `sipClient.getResourceReport()`，封装为 JSON 下载；错误时提示模态。
  - 控制层在失败闭环中继续 `console.group`/`console.table` 输出报告，便于调试。
- 阶段4：文档与改进日志
  - 在三份方案文档中追加“改进结果/使用说明/兼容与回退/不采纳项”章节；更新示例与截图占位。
  - 改进日志明确记录：压测与统计脚本标记为“不采纳”，注明原因与后续条件。
- 阶段5：代码审查与质量验证
  - 代码审查清单：事件键选择、TTL 生命周期、模态模板完整性、按钮权限与可见性、下载文件内容校验。
  - 验证用例：
    - 紧急来电用户挂机→仅一次错误模态、音频停止与队列清空、`getResourceReport().activeAudio==0`
    - 同一失败事件快速触发两次→第二次被去重忽略
    - 导出报告→文件包含 `activeAudio/releases/timeouts/forced/micActive/lastOps`
    - UA断开/ICE失败→仍能走到闭环并输出报告
  - 兼容性：语音合成不可用时仅停音频与蜂鸣；Safari 模态渲染与下载流程验证。

## 稳定性保障措施
- 变更范围收敛：仅紧急失败闭环与调度页新增按钮，不触碰通话核心逻辑与拨号流程。
- 防回归：保留非紧急 `alert()`，模态容器缺失时回退为控制台提示；所有新增逻辑包裹 `try/catch`。
- 观察性：继续结构化输出 `meta` 和 `resourceReport`，便于线上诊断。

## 文档更新清单（按改进点）
- 《紧急拨打模式异常处理与资源释放方案》：新增“事件去重”“模态统一”“资源报告导出”“不采纳：压测脚本”。
- 《紧急拨打异常处理优化与资源释放提升方案》：补充“去重键选择策略”“TTL与并发说明”“按钮操作步骤”。
- 《为振铃组添加启用紧急呼叫…》：补充“调度端导出报告使用说明”“错误模态示例”。

## 代码审查与质量验证机制
- 审查清单：
  - 事件键选择与容错（`call_id/session_id`），TTL 清理无泄漏
  - 模态渲染与隐藏、无阻塞主流程
  - 导出按钮权限与可见性、下载内容与 MIME 正确
  - 紧急音频停止与队列清理在重试 3 次后无残留
- 验证流程：
  - 手测脚本与操作步骤（不包含性能压测）
  - 浏览器矩阵联测与最小兼容保证
  - 变更点回归检查清单随 PR 提交

## 不采纳项（明确标注）
- 性能压测与统计脚本：不采纳
  - 排除原因：用户指令明确排除；压测需独立环境与较大投入；当前目标侧重功能稳定性与可维护性。

## 验收标准
- 紧急失败仅出现一次错误模态，且资源释放两层校验通过；`activeAudio==0`
- 重复失败事件在 5 秒窗口内不重复提示/日志
- 资源报告成功下载，字段齐全且符合当前实现
- 非紧急流程无行为变化，无新增错误日志

## 时间安排与责任分工
- D0：事件去重与模态统一（紧急路径）、联测
- D1：导出按钮与下载逻辑、联测
- D2：文档与改进日志补充、审查与回归测试
- D3：交付验收与小结

如确认上述计划，我将开始实施并在每个阶段完成后提交验证结果与改动摘要。